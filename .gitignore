# ESPHome build output and local state
.esphome/
*.bin
*.elf

# Real secrets never get committed; secrets.yaml.example is the template.
secrets.yaml

# Python bytecode from the external component
__pycache__/
*.pyc
